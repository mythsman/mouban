name: Go Build

on:
  workflow_dispatch:  # 支持手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    
    env:
      GOPROXY: https://goproxy.cn,direct
      GO111MODULE: on
    
    steps:
      # 1. 检出代码
      - name: Checkout code
        uses: https://gitea.com/actions/checkout@v4

      - name: Set up Go
        uses: https://gitea.com/actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Show Go version
        run: go version

      - name: Build
        run: ./build.sh

      - name: Docker login
        run: echo "${{ secrets.REGISTRY_TOKEN }}" | docker login https://gitea.mythsman.com -u "${{ secrets.REGISTRY_USERNAME }}" --password-stdin

      - name: Docker build
        run: docker build -t gitea.mythsman.com/${{ secrets.REGISTRY_USER }}/mouban:latest .

      - name: Docker push
        run: docker push gitea.mythsman.com/${{ secrets.REGISTRY_USERNAME }}/mouban:latest

